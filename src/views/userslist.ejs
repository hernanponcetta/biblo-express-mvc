<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listado de usuarios</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap" rel="stylesheet">
    <link rel="shortcut icon" type="image/x-icon" href="/logo/favicon.ico" />
    <link href="/css/all.css" rel="stylesheet">
    <script src="/scripts/script.js"></script>
</head>
<body class="bg-biblogreen-100">
    <div class="mb-10">
        <%- include('./partials/header') %>
    </div>
    <div class="bg-white font-roboto mb-10 p-3 rounded-md shadow-md text-gray-800 lg:max-w-screen-lg lg:mx-auto lg:px-16 sm:px-5">
        <table class="w-full text-left">
            <tr>
                <th class="text-gray-800 px-2">Id</th>
                <th class="text-gray-800 pr-4 md:table-cell lg:pr-4">Nombre</th>
                <th class="text-gray-800 pr-4 lg:table-cell">Apellido</th>
                <th class="text-gray-800 hidden lg:table-cell">e-mail</th>
                <th class="text-gray-800 hidden lg:table-cell lg:pr-20">DNI</th>
                <th class="text-gray-800 hidden md:table-cell pr-8">Nacimiento</th>
                <th class="text-gray-800 pr-16 hidden md:table-cell">Tipo</th>
                <th>Acciones</th>
            </tr>
            <% users.forEach(user => { %> 
                <tr  class="border-b border-t border-biblogreen-200">
                    <td class="text-xs px-2 py-3"><%= user.id %></td>
                    <td class="text-xs text-left py-3 md:table-cell"><%= user.first_name %> </td>
                    <td class="text-xs text-left py-3 lg:table-cell"><%= user.last_name %> </td>
                    <td class="text-xs py-3 hidden lg:table-cell lg:pr-10"><%= user.email %> </td>
                    <td class="text-xs py-3 hidden lg:table-cell"><%= user.dni %> </td>
                    <td class="text-xs text-left py-3 hidden md:table-cell"><%= user.birth %> </td>
                    <td class="text-xs text-left py-3 hidden md:table-cell"><%= user.userstypes.name %> </td>
                    <td>
                        <div class="w-32">
                            <form action="" method="POST">
                                <button type="submit" formaction="/users/edit/<%= user.id %>?_method=GET" class="text-xs w-full py-2 my-2 font-bold text-gray-800 form-input border-biblogreen-200 focus:shadow-biblofocus focus:border-biblogreen-300 hover:bg-biblogreen-100 focus:outline-none  focus:bg-biblogreen-200">MODIFICAR</button>
                                <button type="submit" formaction="/users/<%= user.id %>?_method=DELETE" class="text-xs mb-2 w-full py-2 font-bold text-gray-800 form-input border-biblogreen-200 focus:shadow-biblofocus focus:border-biblogreen-300 hover:bg-biblogreen-100 focus:outline-none  focus:bg-biblogreen-200">BORRAR</button>
                            </form>
                        </div>
                    </td>
                </tr>    
            <% }); %> 
        </table>
    </div>
    <%- include('./partials/footer') %>
</body>
</html>