<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listado de libros</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap" rel="stylesheet">
    <link rel="shortcut icon" type="image/x-icon" href="/logo/favicon.ico" />
    <link href="/css/all.css" rel="stylesheet">
    <script src="/scripts/script.js"></script>
</head>
<body class="bg-biblogreen-100">
    <div class="mb-10">
        <%- include('./partials/header') %>
    </div>
    <div class="bg-white font-roboto mb-10 p-3 rounded-md shadow-md text-gray-800 lg:max-w-screen-lg lg:mx-auto lg:px-16 sm:px-5">
        <table class="w-full text-left">
            <tr>
                <th class="">Portada</th>
                <th class="text-gray-800 px-2 md:w-48">Título</th>
                <th class="text-gray-800 hidden md:table-cell md:w-32 lg:w-40">Autor</th>
                <th class="text-gray-800 hidden lg:table-cell lg:w-32">Editorial</th>
                <th class="text-gray-800 hidden lg:table-cell lg:w-20">Género</th>
                <th class="text-gray-800 hidden lg:table-cell lg:pr-20">ISBN</th>
                <th class="text-gray-800 pr-8 hidden md:table-cell md:w-20">Precio</th>
                <th class="text-gray-800 pr-8 hidden md:table-cell md:w-20">Estado</th>
                <th>Acciones</th>
            </tr>
            <% books.forEach(book => { %> 
                <tr  class="border-b border-t border-biblogreen-200">
                    <td class="py-3" >
                        <div class="w-12 mr-3 md:w-16">
                            <img src="/portadas/<%= book.book_cover %> " alt="Portada <%= book.title %> "></td>
                        </div>
                    <td class="text-xs px-2 py-3"><a href="products/<%= book.id %>"><%= book.title %> </a></td>
                    <td class="text-xs text-left py-3 hidden md:table-cell"><%= book.autors.name %> </td>
                    <td class="text-xs text-left py-3 hidden lg:table-cell"><%= book.houses.name %> </td>
                    <td class="text-xs py-3 hidden lg:table-cell lg:pr-10"><%= book.genres.name %> </td>
                    <td class="text-xs py-3 hidden lg:table-cell"><%= book.isbn %> </td>
                    <td class="text-xs text-left py-3 hidden md:table-cell"><%= book.price %> </td>
                    <td class="text-xs text-left py-3 hidden md:table-cell"><%= book.states.name %> </td>
                    <td>
                        <div class="w-32">
                            <form action="" method="POST">
                                <button type="submit" formaction="/products/<%= book.id %>/edit?_method=GET" class="text-xs w-full py-2 my-2 font-bold text-gray-800 form-input border-biblogreen-200 focus:shadow-biblofocus focus:border-biblogreen-300 hover:bg-biblogreen-100 focus:outline-none  focus:bg-biblogreen-200">MODIFICAR</button>
                                <button type="submit" formaction="/products/<%= book.id %>?_method=DELETE" class="text-xs w-full py-2 font-bold text-gray-800 form-input border-biblogreen-200 focus:shadow-biblofocus focus:border-biblogreen-300 hover:bg-biblogreen-100 focus:outline-none  focus:bg-biblogreen-200">BORRAR</button>
                            </form>
                        </div>
                    </td>
                </tr>    
            <% }); %> 
        </table>
    </div>
    <%- include('./partials/footer') %>
</body>
</html>